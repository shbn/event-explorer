import React from "react";
// import { useSelector } from "react-redux";

const ShortlistedEvents = () => {
  // Get selected events from the Redux store

  return <div>{/* Render your shortlisted events here */}</div>;
};

export default ShortlistedEvents;
